{
  "name": "sidekick-harvester-examplenode",
  "version": "1.0.0",
  "description": "Example Node Harvester",
  "main": "index.js",
  "directories": {
    "example": "examples"
  },
  "scripts": {
    "build": "./node_modules/.bin/pkg index.js -t node12-darwin-x64,node12-windows-x64 --output build/harvester",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "pkg": "latest"
  },
  "dependencies": {
    "ldk": "git+https://bitbucket.org/crosschx/loop-development-kit-node.git"
  }
}
